/*
inserare element pe prima pozitie

Element *cap=0;
Element *p=new Element;

int val=17;
p->data=val;
p->leg=cap;
cap=p;


Inserare pe o pozitie oarecare

int poz=4;
Element *p=cap
int cnt =1;
while(crt !=3 && p->leg !=NULL// p!=NULL)
{
	cnt++;
	p=p->leg;
}

if(cnt == poz-1)
Element *q = new Element;
int val=100;
q->data;
q->leg=p->leg;
p->leg=q;


Stergerea unui element de pe o pozitie oarecare


int poz =7;
//parcurgere pana la poz-1
Element *q=p->leg;
p->leg=q->leg;//p->leg=p->leg->leg;
delete q;

Elemt *p=cap;
cap=p->leg;//cap=cap->leg;
delete p;
*/